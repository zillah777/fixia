export interface Professional {
    id: string
    name: string
    role: string
    image?: string
    location: string
    rating: number
    reviews: number
    verified: boolean
    tags: string[]
    price: string
    bio?: string
}
